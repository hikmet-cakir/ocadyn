spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://mongodb:27017/ocadyn}

ocadyn:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4321,http://localhost:3000,http://ui:4321}
  scraper:
    base-url: ${SCRAPER_SERVICE_URL:http://scraper-service:9093}
