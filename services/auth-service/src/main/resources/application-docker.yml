spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://mongodb:27017/ocadyn}

ocadyn:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4321,http://localhost:3000,http://localhost:8080,http://127.0.0.1:8080,http://ui:4321}
