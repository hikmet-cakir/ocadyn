import type { UserProfile } from '@/types/user.types';

export const mockUser: UserProfile = {
  id: 'user-1',
  name: 'Alex Morgan',
  email: 'alex@ocadyn.app',
  avatarUrl: undefined,
  plan: 'pro',
  locale: 'en',
};
