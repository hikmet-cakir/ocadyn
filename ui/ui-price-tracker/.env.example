# Leave empty for same-origin `/api/*` requests through the nginx gateway.
# This is the recommended setting for Docker and production deployments.
PUBLIC_API_BASE_URL=

# Used only by `npm run dev` to proxy `/api` to the gateway.
API_PROXY_TARGET=http://localhost:8080
